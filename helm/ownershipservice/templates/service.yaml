apiVersion: v1
kind: Service
metadata:
  name: {{ include "ownershipservice.fullname" . }}
  labels:
    {{- include "ownershipservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ownershipservice.selectorLabels" . | nindent 4 }}
