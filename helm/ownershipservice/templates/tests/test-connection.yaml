apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ownershipservice.fullname" . }}-test-connection"
  labels:
    {{- include "ownershipservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ownershipservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
